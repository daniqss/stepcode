# Variables

Variables can be declared and assigned with the `:=` operator.

```stepcode
n := 5
n := n * 2
b := true
```

Arrays can be declared with square brackets. They are index one based.

```stepcode
arr := [10, 11, 12, 13]
arr[2] := arr[2] + arr[3]
```
